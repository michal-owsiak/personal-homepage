import { Frame, IconWrapper, SunIcon, StyledButton } from "./styled";

const ThemeButton = () => (
  <StyledButton>
    <Frame />
    <IconWrapper>
      <SunIcon />
    </IconWrapper>
  </StyledButton>
);

export default ThemeButton;